This is like [ssh-tunnel-proxy](https://github.com/stefansundin/ssh-tunnel-proxy), but for Kubernetes.

Set the `KUBECONFIG` environment variable to specify a custom kubeconfig.

TODO:
- Read a config file.
- Open tunnels on-demand.
- Socket files.
